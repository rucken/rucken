import { marker } from '@ngneat/transloco-keys-manager/marker';

export class FeatureClientUser {
  static strings = {
    id: marker('FeatureClientUser Id'),
    username: marker('FeatureClientUser Username'),
    password: marker('FeatureClientUser Password'),
  };
}
